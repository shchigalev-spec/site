# Milestone A — Foundation + General Hero

Authoritative cadence: `a-modul-fast-execution-override.md`. Product requirements remain governed by the original A-Modul master specification.

## Required implementation

- preserve the valid repository/source audit and isolated `apps/a-modul` SvelteKit boundary;
- establish official-brand design tokens using Geologica, Onest, IBM Plex Mono, graphite/warm white, official purple `#492D7D`, and official magenta `#E40A46`;
- use the unchanged official Avista logo, not a generated replacement;
- generate three global architectural-CGI anchor candidates with Codex, inspect them, select one continuity family, and document every English prompt/output;
- ship responsive AVIF/WebP derivatives only; no stock, hotlinks, video, baked text/UI/logos, or documentary case implication;
- implement the general route header and exact route-specific hero offer;
- show region context, the primary `Получить КП за 1 рабочий день` CTA, its required qualification, and the secondary tender CTA;
- implement a compact mini-brief collecting object type, either area or personnel, region, and desired commissioning date without faking submission;
- implement the finite site-assembly sequence `empty site → planning grid → module placement → operational object`, with stable start/middle/end, stage controls, and a complete reduced-motion conclusion;
- implement the real-number evidence rail using centralized official facts, without six equal cards;
- create distinct desktop/mobile compositions with the H1 and primary CTA visible in the initial viewport.

## Deterministic gate

Before review, the Builder must prove:

- app starts, typecheck/lint/test/build pass;
- route HTTP 200; root redirects only to `/modulnye-zdaniya/`; canonical is exact;
- zero console/page/request errors and zero broken images;
- no page or element horizontal overflow at 320, 390, 768, and 1440;
- exactly one visible H1 and a visible in-viewport primary CTA;
- all form controls have labels, image intrinsic dimensions exist, and visible interactive targets are at least 44×44;
- mini-brief validation and honest no-submission status work;
- normal-motion stages reach 0→1→2→3 and reduced motion exposes the complete stage without animation dependence.

## Review gate

Invoke exactly two fresh consolidated reviewers after all of Milestone A exists:

1. Visual + Motion Reviewer;
2. Conversion + Factual Reviewer.

Each reviewer must inspect the entire milestone and return a complete P0/P1/P2/P3 issue list. Proceed on `PASS` or `CONDITIONAL PASS` when no P0/P1 remains. P2/P3 items go to `reviews/a-modul-v2/POLISH-BACKLOG.md`. No five-reviewer slice gate applies.
