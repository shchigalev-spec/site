FAIL

# Engineering and factual review — Slice 00, Cycle 3

## Scope actually inspected

- Live route `http://127.0.0.1:5175/modulnye-zdaniya/` in the real in-app browser at 1440×1000 and 390×844, including the rendered desktop/mobile composition, complete visible copy, landmarks, route list, audit counter strip, and public Controlled Launch principle. The route loaded successfully and showed no price, duration, customer/case, leasing-rate, contact, seismic, certificate, or documentary-image claim that would create fake precision in this bounded Slice 00 surface.
- Current evidence: `SPEC.md`, `DIFF-SUMMARY.md`, `TEST-RESULTS.md`, `capture-results.json`, `desktop-1440.png`, `mobile-390.png`, `mobile-320.png`, `mobile-320-classic-client.png`, `start.png`, `mid.png`, `end.png`, `diff-start-end.png`, and the presence/fresh timestamps of `playwright-trace.zip`.
- Current implementation and isolation: `apps/a-modul/src/routes/modulnye-zdaniya/+page.svelte`, root redirect, layout/font imports, `app.css`, A-Modul package/config files, root workspace changes, branch/upstream/base SHA, and the diff against `origin/main`. The branch is `feature/a-modul-direct-landing-v2`; `HEAD` and `origin/main` both resolve to `37004f6e6460203215ed6e57dd396b9421f83eba`; no file under `apps/tech` or `apps/engineering` is changed; no Silent Lab runtime/style/component/asset import or Inter use was found in the A-Modul source.
- Required local specifications and records: the supplied master specification in full, `docs/A-MODUL-SOURCE-FACTS.md`, `docs/A-MODUL-CONTENT-TODO.md`, `docs/A-MODUL-SEARCH-INTENT.md`, `docs/A-MODUL-IMAGE-BIBLE.md`, and `docs/A-MODUL-REVIEW-PROTOCOL.md`.
- Official sources inspected: `https://a-modul.ru/`, `/about/`, `/shift_camps/`, `/production_and_technology/`, `/obshchezhitiya/`, `/koncept/`, `/contacts/`, `/leasing/`, `/modulnye-zdaniya-v-lizing/`, both required object pages, and the required seismic article. The live official sources support the documented 2007 start, 58 settlements, 27,000+ modules, 2,000+ buildings, 305,120 m², 25 modules/shift, current 25,000 m² and 750/month precedence, verified contact/legal defaults, leasing availability without rates, the 300-person/3,200.4 m²/180-module case, the Kamchatka 2,476.36 m² composition/logistics facts, and the locked magnitude-8.8 wording. The official colour logo SVG confirms `#492D7D` and `#E40A46`. The dormitory locality conflict, old 19,100 m² / `от 600` values, no universal price, no generalized duration, BIM caveat, leasing caveat, generated-visualization label, and magnitude-versus-intensity rule are correctly retained in the documents.

## Defect 1 — current TEST-RESULTS does not match the current machine-readable evidence

1. **Exact defect:** the written test record reports a different minimum interactive-target width from the current capture output.
2. **Location:** `reviews/a-modul-v2/slice-00/TEST-RESULTS.md:100` says `145.84×44 px`; `reviews/a-modul-v2/slice-00/capture-results.json:31` records `101.84375` width and line 32 records height `44` for the same `minInteractiveTarget` result.
3. **Evidence:** both files belong to the current Cycle 3 package; `capture-results.json` was captured at `2026-08-25T10:58:56.557Z`, while `TEST-RESULTS.md` calls its browser block the latest Cycle 3 result. The values therefore cannot both be the real current output.
4. **Violated requirement:** Slice 00 requires `TEST-RESULTS.md` to contain commands actually run and their real outputs, and the master review gate forbids stale or retrospective evidence. A reviewer cannot issue PASS on internally contradictory test evidence.
5. **Required correction:** regenerate the complete current evidence package, then copy the actual minimum-target value from the regenerated machine-readable result into `TEST-RESULTS.md` without manual substitution. If the capture script is intended to measure a different target set, correct the script and document that definition, then regenerate all evidence.
6. **Retest:** rerun the real app and the Slice 00 capture command; verify that `TEST-RESULTS.md`, `capture-results.json`, and the rendered screenshots agree on timestamp/current cycle, viewport metrics, and minimum target dimensions; rerun check, lint, test, and build and record only the new real outputs.

## Defect 2 — the mandatory leasing URL status and source-count meaning remain unresolved in a “checked” source inventory

1. **Exact defect:** the supplied mandatory URL `https://a-modul.ru/leasing/` is a direct HTTP 404, not an unverified possible redirect, but the checked source inventory defers the redirect determination and the public Slice 00 counter does not define what its `11` counts.
2. **Location:** `docs/A-MODUL-SOURCE-FACTS.md:29` lists `/leasing/`; line 35 says to keep both leasing URLs “until redirects are checked”; line 134 again defers redirect verification. The same “Minimum official source inventory” contains 12 numbered entries, while `apps/a-modul/src/routes/modulnye-zdaniya/+page.svelte:42` renders `ОФИЦИАЛЬНЫХ URL — 11`.
3. **Evidence:** a fresh `HEAD` request on 2026-08-25 returned `HTTP/1.1 404 Not Found` for `/leasing/`; the official navigation and active content expose `https://a-modul.ru/modulnye-zdaniya-v-lizing/`, which supports only the safe public statement `Возможна поставка в лизинг` without invented rates or schedules. The current inventory therefore has 12 attempted/listed addresses, one dead mandatory address, and 11 live factual source pages after the replacement is included; that distinction is not stated.
4. **Violated requirement:** Slice 00 requires an inspected official-source inventory, source URL list, and conflict/precedence decisions. Leaving a known 404 as an unresolved redirect and showing an unexplained count is not a precise current source audit.
5. **Required correction:** record `/leasing/` explicitly as the supplied mandatory URL returning 404 on the checked date; designate `/modulnye-zdaniya-v-lizing/` as the active official leasing source; state the counting rule unambiguously (for example: 12 attempted inventory entries, 1 dead URL, 11 live official factual sources), and align the live counter and `TEST-RESULTS.md` with that rule.
6. **Retest:** repeat a no-redirect HTTP status check for `/leasing/`, open the active leasing page, verify the corrected source-status/count copy in the live Slice 00 route at desktop and mobile widths, then recreate the full evidence package before invoking fresh reviewers.

Because the current gate package contains a real-output contradiction and an unresolved source-status/count conflict, this Cycle 3 engineering/factual verdict is FAIL. No later-slice acceptance is warranted until both defects are corrected and fresh evidence is reviewed.
