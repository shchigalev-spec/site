FAIL

# Engineering and factual review — Slice 00, Cycle 4

Reviewer role: fresh read-only Engineering and Factual Review Subagent; not the Builder. Review performed 2026-08-25 against the live application, current Cycle 4 evidence, repository state, master specification, all five required Slice 00 documents, and the official A-Modul sources.

## Blocking defect

### 1. Generated workspace output is unignored and omitted from the claimed actual diff

1. **Exact defect:** the repository ignores only root `/node_modules` and does not ignore workspace-local `apps/a-modul/node_modules/`, `apps/a-modul/.svelte-kit/`, or `apps/a-modul/build/`. Consequently, the new application currently exposes 404 generated/cache/build files as ordinary untracked branch content in addition to the 12 intended app source/config files. `reviews/a-modul-v2/slice-00/DIFF-SUMMARY.md` calls itself the “actual diff summary” and enumerates only the intended files, so it is materially incomplete and a later `git add .` would stage generated output that the slice does not disclose.
2. **Location:** repository `.gitignore`; unignored trees `apps/a-modul/node_modules/`, `apps/a-modul/.svelte-kit/`, and `apps/a-modul/build/`; inaccurate inventory in `reviews/a-modul-v2/slice-00/DIFF-SUMMARY.md` under “Minimal runnable A-Modul boundary”.
3. **Evidence:** `git ls-files --others --exclude-standard -- apps/a-modul` returned **416** paths: 172 under `node_modules`, 134 under `.svelte-kit`, 98 under `build`, 7 under `src`, and 5 intended app-root config/package files. `git status --short --untracked-files=all -- apps/a-modul` begins with `.svelte-kit/adapter-node/...` and continues through generated client/server/font artifacts. The current `.gitignore` has `/node_modules` but no recursive/workspace rules for these three trees. By contrast, the diff summary lists only 12 intended app files and does not disclose the 404 generated paths. The generated trees also make an isolated, reviewable branch boundary impossible to establish from the declared diff.
4. **Violated requirement:** Slice 00 requires an independent minimal runnable application boundary and `DIFF-SUMMARY.md` that names the actual changed files. The master review protocol requires reviewers to inspect the real current diff and forbids retrospective or incomplete evidence. The final Git requirements also require a clean, inspected release diff; allowing build products and dependency caches to remain stageable is a hard repository-engineering defect, not a later-slice enhancement.
5. **Required correction:** add repository-appropriate ignore rules (root or app-local) covering workspace `node_modules/`, `.svelte-kit/`, and `build/`; ensure no generated dependency/cache/build artifact is tracked or stageable; then regenerate `DIFF-SUMMARY.md` from the actual post-fix change surface. Do not hide intended source, configuration, documentation, review evidence, or capture-script files.
6. **Retest:** run `git ls-files --others --exclude-standard -- apps/a-modul` and verify it contains only the intended source/config files (plus an intentional app-local `.gitignore`, if that is the chosen fix), with zero paths below `node_modules/`, `.svelte-kit/`, or `build/`. Confirm representative artifacts with `git check-ignore -v`; inspect `git status --short --untracked-files=all`; compare the result against the updated `DIFF-SUMMARY.md`. Because the correction changes the Slice 00 implementation/evidence surface, rerun check, lint, test, build, the live app and all current captures, then invoke five fresh native review subagents.

## Independent checks completed before FAIL

- **Live app:** `http://127.0.0.1:5175/modulnye-zdaniya/` returned HTTP 200; `/` returned a 307 redirect to that route. The live surface showed the same Slice 00 source-control status content as the current desktop/mobile/start/mid/end evidence. It contains no price, duration, customer review, certificate, generated case image, documentary-photo implication, or seismic/design-intensity claim.
- **Current evidence:** inspected `SPEC.md`, `DIFF-SUMMARY.md`, `TEST-RESULTS.md`, `capture-results.json`, 1440 desktop, 390 and narrow mobile captures, start/mid/end, current diff image, and the Playwright trace inventory. Capture time is `2026-08-25T11:15:44.936Z`; the JSON reports one H1, header/main/footer landmarks, zero overflow at 1440/390/320/305, and zero console/page/request failures. `mid.png` and `end.png` are byte-identical because both are at the page’s maximum 836 px scroll position; this is consistent with the recorded metrics and is not a motion claim for this non-interactive slice.
- **Branch/base/isolation:** branch is `feature/a-modul-direct-landing-v2`; fetched `origin/main`, local `main`, and `git merge-base HEAD origin/main` all resolve to `37004f6e6460203215ed6e57dd396b9421f83eba`. No tracked or untracked changes were found under `apps/tech`, `apps/engineering`, or the legacy root runtime directories. A source-only scan of `apps/a-modul/src` and app config found no imports/references to those products, no Inter, and no video.
- **Required documents/master precedence:** read the complete master specification and all of `A-MODUL-SOURCE-FACTS.md`, `A-MODUL-CONTENT-TODO.md`, `A-MODUL-SEARCH-INTENT.md`, `A-MODUL-IMAGE-BIBLE.md`, and `A-MODUL-REVIEW-PROTOCOL.md`. The four-route plan, allowlisted region/type variants, canonical/no-doorway rule, campaign-data limitation, blocked low-ticket `бытовка` traffic, source conflicts, owner-precedence decisions, no-fake-precision rules, no-video rule, and mandatory five-reviewer gate are represented without a contradictory source taking precedence.
- **URL inventory:** direct no-follow status checks independently returned 200 for 11 listed official factual sources and **404 with an empty redirect target** for [the supplied `/leasing/` URL](https://a-modul.ru/leasing/). [The active official leasing page](https://a-modul.ru/modulnye-zdaniya-v-lizing/) returned 200 with no redirect. Therefore the documented counting rule—12 attempted, 1 dead, 11 live—and the live label `ДЕЙСТВУЮЩИХ URL: 11` are accurate. “Возможна поставка в лизинг” is safe; the documents correctly reject invented rates, schedules, approvals, and tax outcomes.
- **Company/production/contact facts:** official pages support `С 2007 года`, 58 operating shift camps, 27,000+ modules, 2,000+ buildings, 305,120 m², 25 modules/shift, project/engineering functions, full-cycle scope, BIM use, and work across Russia/remote regions. The current home/product surface supports 25,000 m² and up to 750 modules/month, while older `/about/` and shift-camp copy exposes `от 600` and/or 19,100 m²; the source memo records the conflict and applies the master’s locked 25,000 / up-to-750 precedence rather than combining values. [Official contacts](https://a-modul.ru/contacts/) support `8 (800) 333-61-31`, `zakaz@a-modul.ru`, ООО «Ависта Модуль Инжиниринг», ИНН 5404491850, ОГРН 1135476132770, and the documented Novosibirsk office address.
- **Cases:** the [300-person dormitory case](https://a-modul.ru/object/obshhezhitija-dlja-prozhivanija-300-chelovek/) supports 3 dormitories, 300 people, 3,200.4 m², 180 modules, three two-storey buildings of 60 modules, transpack delivery, and the documented scope. Its own text conflicts between Nezhdaninskoye/Zabaykalsky context and Republic of Sakha (Yakutia); the source memo correctly locks only the numeric facts and blocks locality use pending confirmation. The [Kamchatka gold case](https://a-modul.ru/object/vakhtoviy-poselok-na-odnom-iz-krupneyshikh-mestorozhdeniy-zolota/) supports February 2022–February 2023, 2,476.36 m², the exact 105/20/12/14/5/4/3/4 composition, engineering/furniture scope, delivery and installation, and vessel-departure logistics from Petropavlovsk-Kamchatsky. Its technical values remain explicitly case-specific.
- **Seismic wording:** the [official seismic article](https://a-modul.ru/article/nadezhnye-i-bezopasnye-modulnye-zdaniya-avista-modul-inzhiniring/) states a 30 July 2025 earthquake of magnitude 8.8 and that Avista-built buildings withstood the seismic load without destruction. The source memo uses the master-locked public sentence exactly, separately states that regional seismic activity is considered during design, and forbids the competing `до 8,6 баллов` wording or conflation with design intensity.
- **Visual-label/brand rules:** the official `color-logo.svg` and `logo-scroll.svg` return 200 and use `#492D7D` and `#E40A46`, matching the image bible. The bible requires Codex-generated architectural CGI, exact live visualization labeling for generated case images, no baked labels/numbers/logos/UI, no stock/competitor imagery, no generated customer logos, separate responsive crops, and no video. No production case imagery exists in Slice 00, so no missing visualization caption is present on the live route.

No later slice should begin until this defect is corrected, all current evidence is recreated, and five fresh reviewers return PASS.
