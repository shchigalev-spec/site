# Motion / Interaction — PASS

Each graph activates through its own one-shot intersection observer, disconnects after the first intersection, and retains its final state. Live inspection showed zero scoped running graph animations after completion and no replay on re-entry.

All variants expose meaningful before/after, peak, reduction, delta, and measurement grammar. Reduced-motion resolves every graph directly to its complete final state with zero running animations.
