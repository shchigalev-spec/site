PASS

Independent reviewers initially failed this slice twice: first because the protocol did not explicitly hard-gate reduced-motion browser evidence, and then because the first reduced-motion capture preserved the previous scroll position and exposed a mobile hero copy collision when corrected to the top.

The protocol now requires mobile, reduced motion, and clean-console evidence. The mobile reduced-motion hero was revised to put core copy in normal flow, recaptured from `scrollY = 0`, and re-reviewed by all four roles. The final evidence shows readable H1/support/actions/conclusion, visible cutaway and selected route, HTTP 200, no overflow, no Vite overlay, and no browser errors. Tech remains frozen and the legacy generator cannot write V2 acceptance evidence.
