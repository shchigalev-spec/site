# Accessibility and Performance — PASS

The final component-scoped axe-core audit returned zero violations. Technical captions are at least 12 px; corrected construction labels measure 5.63:1 and dark-method copy uses explicit accessible colors. Tabs retain roving focus and panel association; state controls expose `role="group"`; each context mounts one titled/described SVG with unique IDs. Controls measure 60/52 px on mobile, overflow is false at 390/320, reduced motion has zero running animations, and source/browser checks are clean.
